# VITS Requirements for Meitei Mayek TTS
# 
# Note: PyTorch is NOT included here to avoid conflicts with pre-installed versions on Cloud GPUs.
# The setup_training.py script will handle PyTorch installation if missing.

Cython>=0.29.21
librosa>=0.9.0
matplotlib>=3.5.0
numpy>=1.21.0,<2.0.0
scipy>=1.7.0
tensorboard>=2.10.0
Unidecode>=1.3.0
huggingface_hub>=0.10.0
setuptools>=60.0.0
wheel>=0.37.0
